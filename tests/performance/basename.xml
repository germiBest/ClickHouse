<test>
    <settings>
        <max_threads>1</max_threads>
    </settings>

    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(basename(URL)) LIMIT 1200000</query>
    <query>SELECT count() FROM hits_10m_single WHERE NOT ignore(basename(Referer)) LIMIT 1200000</query>
</test>
