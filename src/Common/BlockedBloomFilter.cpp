/// Blocked bloom filter — compiled variant selector.
///
/// To switch variants, change the include in BlockedBloomFilter.h
/// and the include below to match.

#include <Common/BlockedBloomFilter_4hash.cpp> // NOLINT(bugprone-suspicious-include)
